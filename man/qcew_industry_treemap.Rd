% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qcew_treemap.R
\name{qcew_industry_treemap}
\alias{qcew_industry_treemap}
\title{Generate QCEW Industry Treemaps}
\usage{
qcew_industry_treemap(
  areas = "32000",
  ownerships = 5,
  period = NULL,
  year,
  include_suppressed = FALSE,
  resolution = "regular",
  breakout_digits = 5,
  sectors = c("2", "3", "4", "5", "6", "7", "8")
)
}
\arguments{
\item{areas}{Character vector of FIPS area codes. Default is `"32000"` (Nevada).}

\item{ownerships}{Numeric vector of ownership codes (e.g., 5 for Private). Default is `5`.}

\item{period}{Character or numeric indicating the quarter/period. Default is `NULL` (determined automatically).}

\item{year}{Numeric year for the data to be pulled. Required.}

\item{include_suppressed}{Logical. Should confidential/suppressed data be included in the plot titles and file paths? Default is `FALSE`.}

\item{resolution}{Character string specifying output plot dimensions. Options are `"regular"` or `"large"`. Default is `"regular"`.}

\item{breakout_digits}{Numeric indicating the NAICS digit level for detailed breakouts (4, 5, or 6). Default is `5`.}

\item{sectors}{Character vector specifying the first digit(s) of NAICS sectors to loop through. Default is `c("2", "3", "4", "5", "6", "7", "8")`.}
}
\value{
Invisible `NULL`. The function creates directories and saves `.png` files as a side-effect.
}
\description{
Downloads Quarterly Census of Employment and Wages (QCEW) data using `BLSloadR`
and generates highly detailed treemaps showing employment levels and wage changes across industries.
Plots are automatically saved as PNG files in a structured local directory.
}
\examples{
\dontrun{
# Generate treemaps for 2025 for Nevada
qcew_industry_treemap(year = 2025)

# Generate treemaps for Idaho (16), Nevada (32), and California (06)
qcew_industry_treemap(year = 2025, areas = c("16000", "32000", "06000"))

# Generate high-resolution treemaps for Las Vegas and Reno MSAs
qcew_industry_treemap(year = 2023, areas = c("C2982", "C3990"), resolution = "large")

# Generate treemaps for California state-owned establishments with 4-digit NAICS detail
qcew_industry_treemap(year = 2025, areas = c("06000"), breakout_digits = 4, ownerships = 2)
}
}
